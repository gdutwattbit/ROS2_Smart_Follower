# 当前推荐运行组合（基于 2026-03-24 ~ 2026-03-26 实车 / VM 回归）

本文只记录 **beta-0.3.0 当前主线** 下，已经在车端或 VM 烟测中验证过、适合直接复用的推荐运行组合。

---

## 1. 推荐组合

### 1.1 感知链路
- 单 worker 异步 perception
- color/depth 同步输入
- depth compare 定位
- tracking / lock / publish 保持单主线

### 1.2 模型配置
- YOLO：`models/yolo26n_static_256x320_simplify_e2e.onnx`
- YOLO 输入：`320x256`
- ReID：`models/osnet_x0_5_512.onnx`
- ReID 输入：`128x256`
- ReID 特征维度：`512`

### 1.3 相机前提
- 相机输入：`640x480 @ 30fps`
- color：`/camera/color/image_raw`
- depth：`/camera/depth/image_raw`
- 内参服务：`/camera/get_camera_info`
- 当前 full bringup 默认：
  - `camera_color_qos:=sensor_data`
  - `camera_enable_depth:=true`
  - `camera_enable_point_cloud:=false`
  - `camera_depth_registration:=true`
  - `camera_enable_d2c_viewer:=false`
  - `camera_enable_ir:=false`

---

## 2. 当前推荐线程参数

```yaml
yolo:
  ort:
    intra_op_num_threads: 1
    inter_op_num_threads: 1
    execution_mode: sequential

reid:
  ort:
    intra_op_num_threads: 1
    inter_op_num_threads: 1
    execution_mode: sequential
```

说明：
- 这不是单帧 benchmark 最激进的组合
- 但它对整条输入链路、发布频率和系统稳定性最友好
- 当前主线目标是 **稳定跟随体验**，不是单帧极限跑分

---

## 3. 当前推荐控制组合

```yaml
follower_controller_node:
  ros__parameters:
    control_rate: 20.0
    target_distance: 0.6
    target_timeout: 0.3
    prediction_horizon_s: 0.25
    velocity_ema_alpha: 0.70
    max_target_speed_mps: 1.50
```

说明：
- 感知约 `10Hz`
- 控制输出 `20Hz`
- 中间依靠 follower 侧短时预测补帧

---

## 4. 当前推荐安装 / 几何参数

```yaml
monocular:
  camera_info_service: /camera/get_camera_info
  camera_x_offset_m: 0.175
  camera_y_offset_m: 0.01

depth_compare:
  min_range_m: 0.20
  max_range_m: 4.00
  sample_window_px: 5
  min_valid_samples: 3
```

说明：
- `camera_x_offset_m`：相机在 base 原点前方为正
- `camera_y_offset_m`：相机在车体左侧为正
- 当前主线不再使用旧单目 `camera_height_m / camera_pitch_deg` 参数

---

## 5. 当前实测摘要

### 5.1 VM 烟测
已验证：
- 全工作区重新覆盖部署后可编译
- `colcon test` 通过
- perception 能成功请求 `/camera/get_camera_info`
- mock color/depth 注入后 `/robot1/person_pose` 可正常发布

### 5.2 实车主线目标
当前推荐组合的目标不是追单帧极限，而是优先保证：
1. 输入链路稳定
2. `/person_pose` 连续
3. follower 输出平顺
4. 锁定 / 恢复行为可控

---

## 6. 为什么当前推荐这一套

1. 320x256 轻量 YOLO 已把检测耗时压到当前平台可接受范围
2. 轻量 OSNet ReID 仍保留了可用的目标区分能力
3. `intra=1` 对整条链路最稳，不会为了单帧速度明显挤压输入和发布
4. depth compare 主线比旧纯单目路线更符合当前实车路线

---

## 7. 当前局限

1. YOLO 依然是主要耗时项之一
2. perception 仍明显占用 CPU，后续仍值得继续做热路径优化
3. 深度定位质量依赖 depth registration、采样窗口和有效样本数
4. 避障在 VM 上因为没有 GPIO，只能做 dry mode 烟测

---

## 8. 下一步优化优先级

1. 继续减少 perception 热路径对输入链路的挤压
2. 继续做实车行为调参
3. 在稳定主线之上再决定是否推进更激进的推理后端优化
