# 当前推荐运行组合（基于 2026-03-24 实机测试）

本文档记录当前阶段在小车主控容器内验证过、综合表现最优的一套感知配置，供后续联调和回归测试直接复用。

## 1. 推荐组合

### 感知链路
- 单 worker 异步 perception
- 最新帧覆盖策略
- tracking / lock / publish 仍在主线程

### 模型配置
- YOLO：`models/yolo26n_static_256x320_simplify_e2e.onnx`
- YOLO 输入：`320x256`
- ReID：`models/osnet_x0_5_512.onnx`
- ReID 输入：`128x256`
- ReID 特征维度：`512`

### 相机前提
- 相机输入：`640x480 @ 30fps`
- 感知输入：`/camera/color/image_raw`
- 当前 full bringup 默认：color-only Astra
  - `color_qos:=sensor_data`
  - `enable_depth:=false`
  - `enable_point_cloud:=false`
  - `depth_registration:=false`
  - `enable_d2c_viewer:=false`
  - `enable_ir:=false`

## 2. 当前推荐线程参数

```yaml
yolo:
  ort:
    intra_op_num_threads: 1
    inter_op_num_threads: 1
    execution_mode: sequential

reid:
  ort:
    intra_op_num_threads: 1
    inter_op_num_threads: 1
    execution_mode: sequential
```

说明：
- `intra=1` 不是单帧 YOLO 最快
- 但它对图像接收链路挤压最小
- 在当前树莓派平台上，能把 `color` 实收与 `/person_pose` 频率拉到最优

## 3. 当前实机摘要

在 color-only + `sensor_data` + 当前默认模型下：

- `ros2 topic hz /camera/color/image_raw`：约 `28.8~29.1 Hz`
- perception `raw_input color`：约 `25.7 Hz`
- `/robot1/person_pose`：约 `9.3~9.5 Hz`
- profile total：约 `92~93 ms`
- profile yolo：约 `55~57 ms`
- profile reid：约 `30~31 ms`

详细数据见根目录 `test.md` 的第 13 节。

## 4. 为什么当前推荐这一套

1. 320x256 静态 YOLO 已把检测耗时压到当前平台可接受范围
2. 轻量 OSNet ReID 能维持可用的目标区分能力，同时不再把总耗时压垮
3. `intra=1` 虽然让 YOLO 单帧更慢，但显著改善了图像接收与发布频率
4. 对整车体验来说，当前最优目标是链路稳定和输出连续，而不是单帧 benchmark 最小

## 5. 现阶段局限

1. YOLO 仍然是主耗时项之一
2. perception 仍会明显占用 CPU，后续仍值得继续做热路径减拷贝和调度优化
3. 单目位置估计还依赖实车安装参数标定质量

## 6. 下一步优化优先级

1. 继续减少 perception 热路径对图像接收链路的挤压
2. 完成单目安装参数实车标定
3. 在这个稳定基线之上再做行为调参与跟随体验调优
