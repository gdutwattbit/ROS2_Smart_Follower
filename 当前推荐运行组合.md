# 当前推荐运行组合（基于 2026-03-19 实机测试）

本文档记录当前阶段在小车主控容器内验证过、综合性能最优的一套感知配置，供后续开发、联调和回归测试直接复用。

## 1. 推荐组合

### 感知链路
- 感知实现：异步版 perception
  - 单 worker 推理线程
  - latest-frame 覆盖策略
  - tracking / lock / publish 仍在主线程

### 模型配置
- YOLO：`models/yolo26n_static_480x640_simplify_e2e.onnx`
- YOLO 输入：`640x480`
- ReID：`models/osnet_x0_5_512.onnx`
- ReID 输入：`128x256`

### 相机前提
- 相机输入：`640x480 @ 30fps`
- 感知输入：`/camera/color/image_raw`
- 当前主链路：纯 RGB + 单目位置估计

---

## 2. 为什么当前推荐这一套

这是目前三轮实机 profiling 中综合表现最好的一组：

1. 异步化后，感知不再被同步回调整段阻塞
2. 新静态 YOLO 模型把 YOLO 耗时明显压低
3. 轻量 OSNet 把 ReID 耗时进一步大幅压缩

最终组合效果：
- 总耗时显著下降
- `/robot1/person_pose` 发布频率显著提升
- 链路运行稳定，没有出现明显卡死或崩溃

---

## 3. 实机结果摘要

### 第三轮（当前推荐组合）
- total：`492.47 ms`
- yolo：`355.76 ms`
- reid：`85.76 ms`
- `/robot1/person_pose`：约 `2.256 Hz`

### 对比最早异步测试基线
对比对象：旧 YOLO + 重型 ReID + 异步

- total：`1031.50 -> 492.47 ms`（`-52.3%`）
- yolo：`471.23 -> 355.76 ms`（`-24.5%`）
- reid：`511.17 -> 85.76 ms`（`-83.2%`）
- person_pose：`0.944 -> 2.256 Hz`（`+139.0%`）

详细数据见根目录 `test.md`。

---

## 4. 运行参数建议

若后续直接拿这套组合作为默认测试配置，建议至少确保以下参数一致：

```yaml
yolo:
  model_path: models/yolo26n_static_480x640_simplify_e2e.onnx
  input_w: 640
  input_h: 480

reid:
  model_path: models/osnet_x0_5_512.onnx
  input_w: 128
  input_h: 256
```

说明：
- 当前主链路仍以 `2048` 维特征接口为主
- OSNet 的 `512` 维输出会补零到 `2048` 维
- 日志出现 `padded to 2048` 属于预期行为

---

## 5. 当前局限

虽然这一套已经是当前最优组合，但仍然存在这些限制：

1. `/robot1/person_pose` 频率仍不算高，后续还需要继续优化
2. YOLO 仍然是主要耗时项之一
3. 若场景中检测框数量上升，ReID 总耗时仍会抬升
4. TF 链如果不完整，`message` 阶段仍会受到影响

---

## 6. 下一步优化优先级

基于当前结果，建议下一步优先做：

1. ONNX Runtime 线程调优
2. 检测频率自适应
3. 更细粒度 profiling
4. 若仍不满足，再评估 INT8 或其他推理后端
