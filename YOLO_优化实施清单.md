# YOLO 优化实施清单

更新时间：2026-03-19
适用版本：当前 alpha-0.1.x 主线
目标：优先降低 `smart_follower_perception` 中 YOLO 推理造成的总时延，并在尽量不破坏现有 ROS2 接口与行为的前提下提升 `/person_pose` 输出频率与新鲜度。

---

## 一、当前结论（基于已完成 profiling）

小车主控实测显示：

- YOLO 约 `465 ms`
- ResNet50 ReID 约 `251 ms`
- OSNet x0.5 ReID 约 `40 ms`
- 更换轻量 ReID 后，总耗时从 `737.75 ms` 降到 `526.53 ms`
- 但 YOLO 耗时几乎不变，说明 **YOLO 已成为当前第一瓶颈**

结论：

1. ReID 轻量化已证明有效
2. 下一阶段优化主攻方向应转向 YOLO
3. 仅继续打磨 ReID，边际收益已明显下降

---

## 二、优化目标

### 2.1 主目标

- 降低 YOLO 单次推理耗时
- 提高 `/robot1/person_pose` 的有效输出频率
- 降低感知输出延迟，而不仅仅是提高平均吞吐

### 2.2 次目标

- 保持现有 ROS2 节点接口、参数命名、消息结构基本不变
- 优先采用“低风险、可回退”的工程改造
- 为后续量化、换后端、异步流水线打好结构基础

---

## 三、优先级清单

# P0：优先做，收益/风险比最高

### P0-1. YOLO 推理异步化（最新帧策略）

目标：
- 将“图像接收/同步”和“YOLO 推理”解耦
- 推理线程始终处理**最新一帧**，旧帧直接丢弃，避免堆积

建议实现：
- 输入侧只缓存 latest synchronized frame
- 单独检测线程或线程池 worker 消费最新帧
- 若上一轮推理未完成，新来的旧帧可直接覆盖
- 跟踪逻辑继续高频运行，检测结果异步注入

收益预期：
- 主观卡顿感下降
- 系统输出更“新鲜”
- 在 YOLO 无法满速跑的情况下，避免延迟不断累积

风险：
- 需要梳理线程安全
- 跟踪与检测结果的时间戳语义要保持清晰

---

### P0-2. 检测频率自适应

目标：
- 不再固定“每 3 帧一次”
- 根据锁定状态/目标状态动态调节检测间隔

建议策略：
- LOCKED + 轨迹稳定：每 `5~6` 帧检测一次
- SEARCH / LOST：每 `2~3` 帧检测一次
- 大角速度/大位移/置信度下降：临时提频

收益预期：
- 显著降低平均 YOLO 调用频率
- 在稳定跟随场景下非常划算

风险：
- 检测间隔变大后，快速运动时可能更依赖跟踪质量

---

### P0-3. 重新导出 YOLO ONNX（静态 shape + simplify）

目标：
- 减少部署图优化阻力
- 保证当前导出的 ONNX 适合固定分辨率边缘端运行

建议导出原则：
- 固定输入尺寸
- `dynamic=False`
- `simplify=True`
- 保持当前 end-to-end 方案，避免额外 NMS 负担

需要验证：
- 当前 `yolo26n.onnx` 是否已按上述方式导出
- 若不是，重新导出并 A/B 测试

---

### P0-4. 输入尺寸 A/B 测试（640 → 512 → 416）

目标：
- 找到“精度够用 + 耗时下降明显”的平衡点

建议顺序：
1. 保持相机输入 `640x480`
2. 模型输入先试 `512`
3. 若识别人体仍稳定，再试 `416`

测试关注：
- YOLO 平均耗时
- `/person_pose` 频率
- 锁定稳定性
- 遮挡恢复质量

说明：
- 这里改的是模型输入，不是必须改相机原始输出
- 对“只跟人”的任务，降低输入尺寸通常很值得试

---

# P1：中期推进，低到中等改造成本

### P1-1. ONNX Runtime 线程参数调优

目标：
- 在主控 CPU 上找到更合适的 ORT 配置

建议测试项：
- `intra_op_num_threads = 2 / 3 / 4`
- `execution_mode = SEQUENTIAL / PARALLEL`
- `graph_optimization_level = ORT_ENABLE_ALL`

注意：
- 小型 Arm CPU 并不是线程越多越快
- 需要实测，而不是凭直觉设大值

建议输出：
- 建立一张 ORT 参数对比表，写入测试文档

---

### P1-2. ONNX Runtime Profiling / Trace

目标：
- 获取 YOLO 推理内部算子级热点

用途：
- 判断瓶颈是否集中在 Conv
- 判断是否存在 transpose / reshape / concat 等额外损耗
- 为后续量化或换后端提供依据

输出建议：
- 保存 profiling trace
- 总结最耗时前几类 op

---

### P1-3. YOLO 运行时预处理优化

检查项：
- resize / letterbox 是否有重复拷贝
- BGR→RGB / HWC→CHW / float32 normalize 是否还能减少一次内存搬运
- OpenCV Mat 到 ONNX input tensor 的填充是否可以更紧凑

目标：
- 把推理前 CPU 预处理成本压缩到最低

说明：
- 当前 profiling 里 `cv_bridge` 不高，但预处理可能仍埋在 `yolo` 阶段里

---

# P2：高潜力，但工程量更大

### P2-1. YOLO INT8 量化

目标：
- 进一步压低 CPU 推理耗时

建议路线：
- 准备一小批代表性图像
- 做 static quantization 优先
- 对比量化前后：
  - person 检测召回
  - bbox 稳定性
  - 跟踪保持质量

风险：
- 会增加模型链路复杂度
- 精度回归必须做

---

### P2-2. 更换推理后端（NCNN / MNN / ACL）

目标：
- 在 Arm 平台上探索比 ONNX Runtime 更优的后端

候选方向：
- NCNN
- MNN
- ONNX Runtime ACL Execution Provider

适用时机：
- 当 P0 / P1 做完后，YOLO 仍明显偏慢
- 或者实测发现 ORT 调参收益有限

风险：
- 接入工作量明显更大
- 调试成本上升
- 维护复杂度提高

---

## 四、建议执行顺序

### 第一阶段（立刻做）

1. 推理异步化 + latest-frame 策略
2. 检测频率自适应
3. 核查并重导出 YOLO ONNX
4. 做 640 / 512 / 416 三档 A/B

### 第二阶段（拿到第一轮收益后）

5. ORT 线程参数调优
6. 开 ORT profiling trace
7. 检查预处理路径

### 第三阶段（若仍不满足）

8. INT8 量化
9. 评估 NCNN / MNN / ACL

---

## 五、实施时的注意事项

### 5.1 我们真正要优化的是“延迟”，不只是平均 FPS

如果 YOLO 很慢，单纯堆线程数不一定解决问题。更重要的是：

- 最新帧优先
- 丢弃陈旧帧
- 跟踪补偿中间帧
- 保证输出尽量反映当前场景，而不是 1 秒前的场景

### 5.2 不建议一开始就同时做太多变量

建议每轮只改一个主变量，例如：
- 只改输入尺寸
- 只改 ORT 线程数
- 只改导出方式

否则结果很难解释。

### 5.3 评估标准不能只看推理时间

每轮 A/B 需要至少同时记录：
- YOLO 平均耗时
- 总 pipeline 平均耗时
- `/person_pose` 实际频率
- 锁定是否稳定
- 丢失恢复是否变差
- 是否引入明显抖动/滞后

---

## 六、建议直接落地的下一步

下一步推荐先做：

1. **感知推理异步化**
2. **latest-frame 覆盖策略**
3. **线程池/工作线程模型梳理**

原因：
- 这是对系统延迟改善最直接的一步
- 也是后续输入尺寸 A/B、ORT 调优、量化的结构前提
- 做完后，后续所有性能实验都会更可信

---

## 七、预期结果

如果 P0 做得顺：

- `/person_pose` 新鲜度会明显提升
- 总体主观跟随体验会比“纯同步串行处理”更好
- 即使 YOLO 本身还不够快，系统也不会一直积压旧帧
- 后续再叠加 512/416 输入和 ORT 调参，才有机会把感知频率继续拉起来
